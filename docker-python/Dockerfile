#запускаем образ
FROM python
#контенер при запуске автоматический перейдет в эту директорию
WORKDIR /usr/src/app
# копируем файлы в рабочуюю директорию в которой запустится котейнер (. это корневая директория проекта ./app копируем из корнево папки app в . рабочуюю директорию образа)
COPY ./app .
# Команда которая будет исполняться при запуске образа представлена в виде массива первый элемент это программа которая будет иполняться,  последующие это ее аргуметы, в нашем случае скрипт
CMD ["python", "main.py"]



